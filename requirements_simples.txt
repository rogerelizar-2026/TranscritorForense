# =============================================================================
# REQUISITOS MÍNIMOS - TRANSCRITOR SIMPLIFICADO
# =============================================================================
# Versão otimizada com menos dependências e mais estabilidade
# =============================================================================

# PyTorch - Deep learning framework
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.6.0

# Diarização de falantes (pyannote.audio)
pyannote.audio>=3.1.0,<4.0.0

# Transcrição com whisperx
whisperx>=3.1.0,<4.0.0

# Identificação de falantes (speechbrain)
speechbrain>=0.5.14,<1.2.0

# Hugging Face Hub (necessário para download de modelos)
huggingface_hub>=0.20.0,<1.0.0

# Processamento de áudio
librosa>=0.10.0,<0.11.0
soundfile>=0.12.0,<0.13.0

# Utilitários
numpy>=1.23.0,<2.2.0
PyYAML>=6.0,<7.0

# Nota: ffmpeg deve estar instalado no sistema (não via pip)
# Ubuntu/Debian: sudo apt-get install ffmpeg
# Windows: choco install ffmpeg ou baixar de ffmpeg.org
# macOS: brew install ffmpeg
